query User {
  authenticatedItem {
    ... on User {
      id
      username
      firstName
      lastName
      email
      profilePicture {
        id
        url
      }
      githubURL
      isAdmin
      websiteURL
      projectsCount
    }
  }
}
