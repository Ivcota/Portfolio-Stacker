query UserProjects(
  $orderBy: [ProjectOrderByInput!]!
  $where: ProjectWhereInput!
) {
  projects(orderBy: $orderBy, where: $where) {
    id
    title
    description
    githubURL
    websiteURL
  }
}
